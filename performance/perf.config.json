{
  "_comment": "성능 도구의 저장소 기본 설정. 코드의 기본값보다 우선하고, 환경변수와 CLI 플래그보다는 뒤진다.",

  "_datasetGuard": "off | warn | strict — 코드 기본값은 off 지만 이 저장소는 strict 로 운영한다. large 데이터셋은 재현이 안 되는 일회성 자산이라(시더의 난수 소비 순서 문제) 오염되면 되돌릴 방법이 스냅샷뿐이다. 상세: tools/lib/guard.js",
  "datasetGuard": "strict"
}
