# Legacy performance documents archived on 2026-09-11

이 디렉터리는 `EXP`, `BTL`, `OPT`, 대형 findings 문서 중심의 이전 구조를 그대로 보존한다.
한 문제의 수치와 판단이 여러 파일에 중복되고, 오래된 실행과 현재 코드가 섞이는 문제가 있어
정본에서 제외했다.

현재 문서는 [performance/README.md](../../../README.md)에서 시작한다. 이곳의 내용은 새 문서에
없는 조사 단서를 찾을 때만 사용한다.

