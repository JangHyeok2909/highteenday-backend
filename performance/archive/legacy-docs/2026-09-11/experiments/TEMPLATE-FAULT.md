# EXP-XXX: (장애 관측 실험 제목)

> 상태: 계획 | 실행 완료 | 해석 중 | 완료 | 재실험 필요
> 날짜: YYYY-MM-DD | 계획 파일: `resilience/faults/XXX.json` | 관련: KI-XX, BTL-XXX

<!--
이 양식은 **장애 관측 실험** 전용이다. 성능 개선 실험은 TEMPLATE.md 를 쓴다.

왜 따로 있는가 — 두 실험은 질문이 다르다.
  성능 개선: "어제보다 느려졌는가" → 기준선(다른 실행)과 비교, PASS/FAIL 판정, Before/After
  장애 관측: "이 장애에서 무엇이 어떻게 깨지는가" → 기준은 같은 실행의 pre 구간, 판정 없음

그래서 TEMPLATE.md 의 6절(판정 기준)·11절(Before/After)이 여기에는 없다. 대신 §3(가설 대
관측)과 §8(관측하지 못한 것)이 있다. 자세한 사정은 resilience/README.md.

**이 문서를 쓸 때의 규칙**
  1. §2·§4·§5 의 표는 report.html 에서 **그대로 옮긴다**. 옮기면서 반올림하거나 고르지 않는다.
  2. 사실(보고서에 있는 값)과 추론(내가 이은 것)과 권고(고칠 것)를 문단으로 섞지 않는다.
     각 절이 그 셋 중 무엇인지 머리에 적혀 있다.
  3. 보고서에 없는 값을 쓰지 않는다. 필요하면 다시 돌린다.
  4. "모른다"는 결론이다. §8 에 적는다. 빈칸으로 두면 나중에 안 쟀다는 사실이 사라진다.
-->

## 1. 질문

<!-- 계획 파일의 `question` 을 그대로. 이 실험이 답하려는 것 한 문장. -->

## 2. 실행 신원 — 무엇을 잰 실행인가

> **사실.** report.html 의 10·11·12번 절에서 그대로 옮긴다.

| 항목 | 값 |
|---|---|
| 실행 ID / 보고서 | `<runId>` — [report.html](../../resilience/reports/<runId>/report.html) |
| 커밋 | `xxxxxxxx` (+dirty 여부) |
| 앱 이미지 | `xxxxxxxxxxxx` · 빌드 YYYY-MM-DD · 소스와 일치 / **소스보다 낡음** |
| 부하 스크립트 지문 | `xxxxxxxxxxxx` |
| 데이터셋 | `medium` (지문 `xxxxxxxxxxxx`) |
| 구간 · 도착률 | pre Xs / fault Xs / post Xs · X/s (preVUs X, maxVUs X) |
| 연결 경로 | 직결 / toxiproxy 경유 |
| 주입 | 계획 Xs → 실제 Xs (오차 Xs), 결과 ✓/✗ |
| 타임아웃 설정 | 커넥션 획득 X · Redis 명령 X · 소켓 X — **가정 N건** |

**이 실행의 수치를 그대로 믿으면 안 되는 이유** (보고서 맨 위 "주의" 블록을 옮긴다. 없으면 "없음"):

<!-- 예: dropped_iterations 가 N 이라 fault 구간의 오류율 분모가 계획보다 작다 -->

## 3. 가설 대 관측

> **사실 + 판단.** 계획 파일의 `expect` 를 그대로 옮기고, 각각 옆에 관측을 적는다.
> 채택/기각은 이 표에서만 쓴다. 도구는 판정하지 않으므로 여기가 사람이 판정하는 유일한 자리다.

| # | 가설 (계획의 expect) | 관측 (보고서 어느 절의 무슨 값) | 판단 |
|---|---|---|---|
| 1 | | | 채택 / **기각** / 확인 불가 |
| 2 | | | |

**확인 불가**로 적은 것은 §8 로 옮긴다 — 왜 확인하지 못했는지가 다음 실험의 설계를 바꾼다.

## 4. 무슨 일이 일어났나 — 시간순

> **사실.** 보고서 3번 절(시간축)과 2번 절(주입 기록)을 시각 순으로 읽어 서술한다.
> 여기서는 아직 원인을 쓰지 않는다. "무엇이 언제 어떻게 변했는가"만 쓴다.

| 시각 | 사건 · 관측값 | 근거 (보고서 절) |
|---|---|---|
| `0~Xs` | pre 구간 기준: p95 Xms, 오류율 X%, Hikari active X, Tomcat busy X | 4·8번 |
| `Xs` | 주입: (무엇을) | 2번 |
| `Xs` | | |
| `Xs` | 복구 주입 | 2번 |
| `Xs` | | |

## 5. 인과 사슬 — 왜 그렇게 됐나

> **추론.** 위 관측들을 잇는다. **각 고리마다 근거가 되는 보고서의 절과 값을 붙인다.**
> 근거를 못 붙이는 고리는 추측이므로 §8 로 내린다.

1. (관측) … — 근거: N번 절, 값 X
2. (그래서) … — 근거: N번 절, 값 X
3. (그래서) … — 근거: N번 절, 값 X

**한 문장 요약**:

<!-- 예: "트랜잭션 안에서 Redis 를 호출하는데 Redis 타임아웃이 60초라, 커넥션 하나가 60초 묶인다" -->

**이 사슬을 반증하려면**: <!-- 어떤 관측이 나오면 이 설명이 틀린 것인가. 다음 실험의 설계가 여기서 나온다 -->

## 6. 폭발 반경

> **사실 + 추론.** 보고서 7번 절(기능 × 구간)에서 옮긴다.

| 영향 | 기능 | 근거 |
|---|---|---|
| 장애 의존성을 **쓰는** 기능 | | |
| 장애 의존성을 **안 쓰는데 같이 죽은** 기능 | | |
| 영향 없음 | | |

**경계를 넘은 경로**: <!-- 공유 자원이 무엇이었나 — 커넥션 풀 / 워커 스레드 / 락 -->

## 7. 회복

> **사실.** 보고서 4·8번 절의 post 구간과 3번 절 그래프.

| 항목 | pre | fault | post | 회복됐나 |
|---|---|---|---|---|
| 오류율 | | | | |
| p95 | | | | |
| (자원 지표) | | | | |

**복구 조작 이후 정상화까지**: <!-- 그래프를 눈으로 읽어 시각을 적는다. 도구가 아직 계산해 주지 않는다 -->
**복구 뒤에도 남은 것**: <!-- 빈 캐시, 잔여 대기, 히트율 저하 등 -->

## 8. 관측하지 못한 것

> **사실.** 이 절을 비워 두지 않는다. 못 잰 것을 적어야 다음 실험이 설계된다.

| 알고 싶었는데 못 알아낸 것 | 왜 못 알아냈나 | 알아내려면 |
|---|---|---|
| | | |

## 9. 결함으로 승격할 것

> **권고.** 관측이 아니라 판단이다. 여기서 나온 항목은 실제로 `docs/KNOWN-ISSUES.md` 나
> `bottlenecks/` 에 등록하고, 등록한 번호를 여기에 되적는다 — 장부와 문서가 갈라지면
> 둘 다 못 믿는다 (KI-01 의 "기록 정정" 참고).

| 내용 | 심각도 근거 (관측값) | 등록처 |
|---|---|---|
| | | KI-XX / BTL-XXX / 등록 안 함(이유) |

## 10. 다음 실험

<!-- §5 의 반증 조건과 §8 의 못 잰 것에서 나온다. 계획 파일까지 만들었으면 경로를 적는다. -->

## 11. 재실행 (고친 뒤)

> 같은 계획 파일로 다시 돌린 실행이 있으면 링크만 둔다. **수치를 나란히 놓는 것은 사람이 한다** —
> 도구는 자동 비교를 만들지 않는다(resilience/README.md).

| 실행 | 커밋 | 무엇을 바꿨나 | 보고서 |
|---|---|---|---|
| Before | | | |
| After | | | |
